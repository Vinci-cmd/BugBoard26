# --- CONFIGURAZIONE SERVER ---
server.port=8080

# --- COLLEGAMENTO A RENDER (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://dpg-d4k1rbu3jp1c738hgfkg-a.frankfurt-postgres.render.com:5432/bugboard26_vyc6?ssl=true&sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory
spring.datasource.username=bugboard26_vyc6_user
spring.datasource.password=qbafm5YYZ0T6lmXgd3AP4fFLEPOpgvVu
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA & HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Log SQL: Utile per vedere le query generate nella console (Debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- FORMATTAZIONE JSON (DATE) ---
# Imposta il formato data leggibile (es. 2025-10-25 14:30:00)
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=Europe/Rome
# --- GESTIONE UPLOAD FILE ---
# Aumentiamo il limite per singolo file (es. 10MB)
spring.servlet.multipart.max-file-size=10MB
# Aumentiamo il limite totale della richiesta (es. 10MB)
spring.servlet.multipart.max-request-size=10MB
# --- CONFIGURAZIONE SWAGGER (SpringDoc) ---
# Percorso per la documentazione JSON (utile per Postman o frontend generator)
springdoc.api-docs.path=/api-docs

# Percorso per l'interfaccia grafica (UI)
springdoc.swagger-ui.path=/swagger-ui.html

# Ordina gli endpoint in ordine alfabetico (più facile da leggere)
springdoc.swagger-ui.operations-sorter=alpha
springdoc.swagger-ui.tags-sorter=alpha

# Disabilita il try-it-out di default se vuoi (opzionale)
# springdoc.swagger-ui.supported-submit-methods=get, post, put, delete, patch